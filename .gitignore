# Cursor IDE (local)
.cursor/

# Reference notes (local)
references/

# Markdown docs (local notes; regenerate or keep outside git)
*.md
!README.md
!**/README.md

# CodeGraph local index (regenerate with `codegraph index`)
.codegraph/

# Node
node_modules/

# Rust
target/

# Env / secrets
.env
.env.*

# Bundled Whisper weights (fetch locally; see jarvis/scripts in later tasks)
*.bin

# Tauri / Vite output (also per-package .gitignore under jarvis/)
dist/
/.agents
