# Cargo is often invoked from `jarvis/` (rust-analyzer, npm scripts). Whisper env lives under
# `src-tauri/.cargo/` — pull it in here so bare `cargo check --manifest-path src-tauri/Cargo.toml` works.
include = ["../src-tauri/.cargo/config.toml"]
